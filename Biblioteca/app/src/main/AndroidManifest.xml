<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aluno.biblioteca">

    <application
        android:name=".model.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListaLivrosActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CadastroLivroActivity"
            android:parentActivityName=".ListaLivrosActivity" />
        <activity
            android:name=".CadastroUsuarioActivity"
            android:parentActivityName=".LoginActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".DesejadoActivity"
            android:parentActivityName=".ListaLivrosActivity"/>
        <activity
            android:name=".LendoActivity"
            android:parentActivityName=".ListaLivrosActivity"/>
        <activity
            android:name=".LidoActivity"
            android:parentActivityName=".ListaLivrosActivity"/>
    </application>

</manifest>